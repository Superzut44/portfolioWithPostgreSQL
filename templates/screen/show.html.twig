{% extends 'base.html.twig' %}

{% block title %}Screen{% endblock %}

{% block body %}
    <div class="marginTop"></div>
    <h1>Screen</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ screen.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ screen.name }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ screen.image }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_screen_index') }}">back to list</a>

    <a href="{{ path('app_screen_edit', {'id': screen.id}) }}">edit</a>

    {{ include('screen/_delete_form.html.twig') }}
{% endblock %}

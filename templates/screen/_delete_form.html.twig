<form method="post" action="{{ path('app_screen_delete', {'id': screen.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ screen.id) }}">
    <button class="btn">Delete</button>
</form>
